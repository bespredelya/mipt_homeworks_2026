[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=gigavibe --cov-report=term-missing --cov-report=html"

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true

